.nested_fields 
    .field 
        = f.collection_select :product_id, Product.order(:name), :id, :name, prompt: 'Selecione um produto'
        = f.number_field :quantity 
    .remove 
        = link_to_remove_association "| X |", f